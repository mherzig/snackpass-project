import { resetAndSeedTickets } from '../../data/reset'

/**
 * Clears current tickets and seeds a bunch of new ones dated within the last 48 hours.
 */
const seedTickets = resetAndSeedTickets

export default seedTickets